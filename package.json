{
  "name": "markethub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix server && npm run migrate --prefix server && npm run seed --prefix server",
    "start": "npm run start --prefix server",
    "preview": "cd client && npm run preview",
    "migrate": "npm run migrate --prefix server",
    "migrate:run": "npx sequelize-cli db:migrate --prefix server",
    "seed": "npm run seed --prefix server",
    "seed:run": "npx sequelize-cli db:seed:all --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
