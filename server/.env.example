# Server Configuration
NODE_ENV=development
PORT=5000

# Application Name
APP_NAME=Stylay

# Database Configuration (MySQL - for local development)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=stylay_db
DB_NAME_TEST=stylay_db_test
DB_USER=root
DB_PASSWORD=your_db_password_here
DB_DIALECT=mysql

# Database Configuration (PostgreSQL - for production/Vercel)
# DATABASE_URL=postgresql://neondb_owner:npg_MWXlHG2mAOx8@ep-solitary-haze-ad801ncd-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
# DB_DIALECT=postgres

# Database (Clever Cloud MySQL addon)
MYSQL_ADDON_HOST=
MYSQL_ADDON_DB=
MYSQL_ADDON_USER=
MYSQL_ADDON_PASSWORD=
MYSQL_ADDON_PORT=3306

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60 * 60 * 1000 # 1 hour
RATE_LIMIT_MAX=1000

# Security - CORS Origins (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

# Logging
LOG_LEVEL=info

# Logo Configuration
LOGO_URL=http://localhost:5000/logo

# Email Configuration (for future use)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password
EMAIL_FROM=noreply@stylay.com
EMAIL_FROM_NAME=Stylay

# AWS S3 Configuration (for file uploads)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=your_aws_region
AWS_BUCKET_NAME=your_bucket_name

# Frontend URL (for CORS and email templates)
FRONTEND_URL=http://localhost:5173
ADMIN_URL=http://localhost:5173/admin
VENDOR_PORTAL_URL=http://localhost:5173/vendor

# Support Email (for email templates)
SUPPORT_EMAIL=support@stylay.com

# Session Secret (for express-session)
SESSION_SECRET=your_session_secret_here

# Password Reset Token Expiration (in minutes)
PASSWORD_RESET_EXPIRES=30

# Default User Type Passwords
DEFAULT_VENDOR_PASSWORD=Vendor@123
DEFAULT_CUSTOMER_PASSWORD=Secret123!
DEFAULT_ADMIN_PASSWORD=Admin@123
DEFAULT_SUBADMIN_PASSWORD=SecurePass123!

# Payment Configuration
PAYSTACK_SECRET_KEY=sk_test_your_paystack_secret_key
PAYSTACK_PUBLIC_KEY=pk_test_your_paystack_public_key
PAYSTACK_BASE_URL=https://api.paystack.co
PAYSTACK_WEBHOOK_URL=http://localhost:5000/api/v1/webhooks/paystack
PAYSTACK_CALLBACK_URL=http://localhost:5000/api/v1/orders/verify

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here

# Redis (Clever Cloud Redis addon)
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=
